# ListCleaner 1.4.5 (19)

## 修复依据

诊断显示界面 18、system/Resolver Hook 17 (STALE)，本地 0 条规则而 system 仍使用 92 条规则，管理 UID 10715 的查询未豁免。此版本不再把“远程偏好写出成功”称为运行成功。

## 修改

- 同步与扫描统一走序列化启动流程。存在 STALE/FAILED/RELOADING 或版本码不一致的运行目标时，不向旧代码提交新配置，不用可疑扫描覆盖管理目录。必须有可用 system 目标。
- 本地是否存在配置按持久化键判定，不按规则数判定；已有的空规则不是数据丢失。无本地配置但有远程配置时，等待用户明确选择恢复或重置（重置有确认框，并切为 SHOW_ALL）。不自动恢复，不静默覆盖。远程数据已经丢失时不能恢复过去的规则。
- 配置以单个 JSON 写入。管理应用使用仅查询、不启动组件的私有 Intent 向 system Hook 请求版本+配置 SHA-256。Hook 只对框架配置中的管理 appId 对应的 Binder 调用者返回确认，不信任 Intent 传入的身份或规则。确认仅说明系统查询 Hook 的当前快照，不代表 Resolver 侧所有行为已经验证。
- 私有查询返回的 RuntimeProbe 是探测标记，不是可启动 Activity；不注册导出组件、端口或额外权限。普通调用者不能通过它绕过过滤或修改规则。
- 规则、管理身份、摘要合并为一个不可变快照；空 key 通知也处理。每两秒至多在查询时检查一次已有偏好快照，防止仅依赖监听回调；这不是跨进程缓存强制刷新保证。确认失败时阻止扫描，并提示重启。
- API102 热重载：旧代注销偏好监听，新代显式读取配置并原子替换每个查询/排序 Hook。基于旧 Hook 的宿主方法 ClassLoader 发现新增 Hook，不假设启动回调会重放。不传递旧模块类对象。
- 自动 APK 更新热重载开启；界面提供“检测并应用模块更新”，按框架返回的目标句柄请求，不用 PID 构造目标。处理拒绝、不支持、目标退出、正在重载和超时，随后重新检测。不杀 system_server。
- 每个 Hook 的替换是原子的，整个 Hook 集合不是事务。部分失败会报告失败；不承诺自动回滚。框架失败后如保留旧代码，其按需偏好检查仍可工作，但界面会要求重启。自动更新也依赖框架实际支持。
- 历史扫描目录保存名称、分类与少量匹配依据（最多5000条、2MB字符）；刷新缺失项标为未确认，不直接移除。它不是有效性保证，也不会恢复被系统清除的缓存。图标从 PackageManager 重读。
- 列表和勾选、视图条件使用同一组合快照。本会话见过的已选高级组件取消勾选后仍保留；重启应用后高级开关仍有其原本含义。分类和搜索始终有效。
- 诊断增加配置摘要、系统确认状态、恢复待决状态、视图条件、候选数和显示组数；不记录搜索内容，不上传手机日志到仓库。

## 首次升级

1. 如有规则先导出备份。
2. 本地编译并覆盖安装 1.4.5。
3. 从 1.4.4 或更早版本迁移，必须完整重启一次：旧代 onHotReloading 默认拒绝，不能用新版反向改变旧代行为。
4. 打开应用。如提示本地配置缺失，选择恢复或确认重置，然后刷新。
5. 检查 system 和已运行 Resolver 均为 UP_TO_DATE/19，状态显示系统已确认配置。

## 回归验收

- 勾选浏览器后刷新：管理入口仍在，系统菜单按规则处理。
- 清除 ListCleaner 数据（备份后测试）：存在远程配置时不先覆盖，先提示恢复/重置。
- 旧 Hook 仍在运行：显示旧版本并暂停扫描，不把少量候选当成完整目录。
- 后续版本升级：支持的框架尝试热重载；系统与 Resolver 分别核验，失败需重启。
- 全部显示下取消普通勾选不移除应用；取消本会话已管理的高级项不突然消失。
- 历史候选在本次扫描缺失时保留名称/图标、标记未确认；重新匹配后去掉未确认标记。

## 验证边界

Java 独立检查可在无 Android SDK 的环境执行；Android/Kotlin 编译、真机热重载和厂商兼容性仍需要本地验证。不附 SDK 路径、签名或新构建环境；没有把 Root 作为配置传输渠道。

本次执行：原有106项 Java 检查、12项运行协议检查及诊断去重/边界检查通过。新增3项 Kotlin 列表回归用例已写入，但当前环境未运行 Android Gradle/JUnit。不能将这些检查等同于 APK 编译或真机热重载通过。

接口依据：libxposed API102 XposedModuleInterface.onHotReloading/onHotReloaded、HookHandle.replaceHook，以及 Service102 hotReloadModule/HotReloadResult；配置变更不触发代码热重载。
