# ListCleaner 1.4.6 (20)

## 已修复

- 目录发现与菜单查询分开：目录使用 MATCH_ALL，文本外同时要求 DEFAULT 类别；实际文件查询保留菜单 flags。浏览器扫描记录窄查询对照数量、解析目标及查询 flags，不将对照目标断言为浏览器角色持有者。
- 宽泛类型匹配与权限限制分开。宽泛匹配显示证据，不表示实际文件可启动；受限组件默认隐藏，用户可单独查看。
- 历史目录默认隐藏，折叠应用行也标注历史数量。未配置历史7天到期；已配置失效项保留取消入口。旧缓存缺少时间戳时从本次迁移起计时。未命中不等于卸载，不自动删除用户规则。
- 移除 seenSelected 会话特例，历史/受限开关持久化。取消受限或历史规则后按同一视图条件隐藏；开启对应开关可重新查看，重启前后行为一致。
- 部分探针失败不再丢弃全部成功结果，显示不完整警告；失败对应的旧候选作为未确认历史记录。
- 缓存优先保留已配置条目和新鲜候选，有界裁剪并记录结果；读取失败、提交失败不再静默。规则仍独立保存，缓存裁剪不裁剪规则。
- 排序新增候选排除仅历史/受限应用；已保存旧排序仍可移除。
- 远程配置解析失败时仍提供明确确认的重置入口，不要求先恢复成功。解析与写入增加配置长度边界。
- SHOW_ALL 可发送给明确兼容的19/20版 UP_TO_DATE/STALE目标；不绕过未知版本、FAILED、RELOADING保护。扫描仍要求所有运行目标当前版本和system摘要确认。未确认时不声称暂停成功。
- 导出诊断直接收集最近状态和原始日志，不等待同步锁或框架查询；附状态观察时间、缓存结果、显示决策、宽泛/受限/历史依据。模块证据识别配置确认及热重载结果。
- 热重载前校验旧查询句柄；每个目标的请求异常单独报告，不中止其余目标。未变化配置轮询不重复计算摘要。
- 相对类名读取、勾选写入统一规范化，避免组件id相同但集合判断不同。

## 保留的边界

- API102热重载仍是逐Hook替换，不是全局事务；部分失败仍可能需要重启。未实现或宣称整代自动回滚。
- 轮询已有远程偏好对象不保证强制刷新框架缓存，system确认不等于Resolver确认。
- Binder调用本身没有可安全强制取消的保证。本版保证诊断不依赖这些调用，不宣称解决所有底层IPC挂起。
- MATCH_ALL不是全设备所有用户、所有域名、所有文件类型的穷举保证；不扩大作用域到所有应用。
- 非文本分类空结果恢复保护仍保留，文本允许全部隐藏。

## 验证

新增Kotlin回归覆盖：目录flags、真实文件flags、历史隔离、已配置保留、历史过期、宽泛与受限区别、取消选择视图一致性、相对类名。
独立Java检查覆盖暂停协议版本/状态白名单等纯逻辑；Android/Kotlin构建、真实默认浏览器、多用户、框架热重载需本地编译和真机验证，不宣称已通过。

## 安装后验收

1. 覆盖安装，使用“检测并应用模块更新”；框架不支持或失败则重启。确认system及已运行Resolver为UP_TO_DATE/20。
2. 刷新浏览器目录，检查非默认浏览器；无需清除ListCleaner数据。
3. 勾选、取消、刷新和重启应用，核对普通条目不因选择而消失；历史/受限项按开关显示。
4. 测试暂停、恢复规则与实际文件检查；异常直接导出诊断，不需要先同步成功。
