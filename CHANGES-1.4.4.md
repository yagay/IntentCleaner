# 1.4.4：合并分类问题与无效补丁分析

## 已实现

1. 管理身份不再使用开机时返回-1的moduleApplicationInfo。应用使用自身UID生成appId，通过框架管理的远端配置同步；Hook只信任该配置，不接受Intent extra身份。身份未知时系统过滤暂停并记录FILTER_PAUSED；收到有效身份后记录MANAGER_IDENTITY，首次管理查询记录MANAGER_QUERY_BYPASS。
2. 新增共享IntentClassification和Intent.intentKind，扫描、系统查询、Resolver查询和最终排序共用。系统查询使用Binder参数里的resolvedType补充Intent.type，避免content实际已解析MIME却被当未知。
3. 自定义协议upi/msauth/geo/tel等不进入文件规则。明确PDF/媒体MIME的HTTP(S)归文件查看；普通网页、HTML归浏览器。无法判断的content和cursor数据请求保持原样，不猜分类。
4. 扫描普通候选使用MATCH_DEFAULT_ONLY（PROCESS_TEXT按查询菜单语义使用0），宽泛探针使用MATCH_ALL并标记高级。未导出、未启用、模块无所需调用权限的返回项也标记高级。高级不是建议隐藏名单，不自动添加规则。
5. 扫描保留每个组件的匹配依据（最多32条），合并时优先普通/实际文件依据；提供每个探针raw/kept/new统计，判断探针是否仅重复返回结果。新增具体文件名、content/file/https样例，不承诺穷举全部URI。
6. 已配置但漏扫的规则仍显示占位项；分类计数、标签、批量勾选统一基于当前可见组件。高级开关不隐藏已配置项，但搜索/只看未选等视图条件仍适用。
7. 新增实际文件检查：通过系统文档选择器选择一个文件，仅读取MIME并queryIntentActivities，不调用目标、不读取正文、不记录完整URI。候选合并到列表并标明实际检查依据。文档提供者返回的URI或调用身份可能不同于原文件管理器，因此不保证菜单完全一致。
8. 移除查询阶段预排序，保留排名结束后的AOSP适配入口。取不到Intent、未分类、未配置优先顺序都会记录ORDER_SKIP；未知实现不修改原结果。系统和Resolver仍有各自过滤，ThreadLocal只用于本进程线程的嵌套调用保护，不声称跨进程全链路去重。
9. 关键日志与候选明细分开限流；首次堆栈按层/分类/调用UID分组，管理扫描和显式查询不占用首次堆栈名额。
10. 诊断证据去重、分组，保留来源/PID、丢弃和截断状态。新增完整扫描快照与探针统计。摘要是历史观察，不伪装成实时健康检测。
11. 清理无调用isDiagnosticMode、PriorityDialog包装和仅旧测试使用的groupCandidates重载；测试改用正式入口。停止重复写入旧远端配置键，保留旧配置读取和版本1—3 JSON备份导入。更新必须重启，旧模块进程不承诺继续热接收新格式。

## 保留且没有盲目删除

系统启动Hook和继承方法发现、Resolver过滤兜底、原子配置、异常放行、同UID保护、跨资料排序、文件空列表保护、日志容量和超时保护。旧系统类探测仍作为兼容分支保留。

## 行为边界

- 不承诺所有扫描结果一定能正常启动；正常列表表示更接近外部打开条件的样例匹配，不是对任意文件的可用性认证。
- 开关高级候选只是视图，不改变Hook规则。隐藏规则仍按分类+组件生效，不按文件后缀单独生效。
- 不给自定义协议新增隐藏规则，不拦截显式启动。迁移不删除已有OPEN规则，但它们不再作用于自定义协议。
- 保留文件/分享/浏览器的空列表恢复保护；PROCESS_TEXT可以为空。
- 不强制改变用户资料可见性、其他模块过滤、默认应用、硬编码菜单或系统缓存。
- 身份未同步时必须打开应用提交配置；应用设置与框架连接失败不会伪报豁免成功。
- 排序仅保留已探测到的排名结束入口；没有命中证据时属于未验证，不回退到可能被覆盖的预排序。

## 本轮验证

已运行25项分类、7项管理身份、50项过滤策略、24项日志缓冲区检查。另运行证据去重/计数/无关日志排除/容量边界测试；用用户12:52诊断包的两份框架日志回放，去重667条，保留system和Resolver的独立计数。

新增JUnit：原子配置带管理身份往返、漏扫规则可管理、普通依据提升高级候选、实际文件依据排序与去重。没有执行完整Android/Kotlin构建和真机验证，不能将纯Java测试视为整机功能通过。

## 一次验收

1. 导出备份，用原签名安装18版。打开应用提交配置，再重启手机并刷新列表。
2. 开启诊断。核对MANAGER_IDENTITY appId=实际值，以及MANAGER_QUERY_BYPASS；不得长期处于FILTER_PAUSED。
3. 隐藏Acode等目标后刷新，确认管理入口仍在，取消勾选能恢复。对未扫描到的旧规则确认有占位说明。
4. 查看普通/高级候选差异及组件匹配依据，用实际TXT/PDF/APK各检查一次；按当前分类核对计数和批量选择范围。
5. 复现网页、文件打开、分享、多文件分享、文本处理；自定义登录/支付协议不应套用OPEN过滤。
6. 若要验证排序，先配置两项优先应用；检查ORDER_APPLIED与changed，或明确ORDER_SKIP理由。
7. 最后导出一个ZIP，查看scan-probes、scan-candidates、real-file-probe和去重后的module-evidence。附操作时间和仍有异常的具体组件名称。
