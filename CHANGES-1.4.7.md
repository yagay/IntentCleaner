# Intentcleaner 1.4.7 (21)

## 管理方式简化

借鉴 Tiger 的“扫描 → 分类 → 勾选 → 保存”交互，继续使用现有 LSPosed 后端，不引入 IFW。

- 删除全量候选持久化缓存、缓存读写/裁剪状态、历史合并、最后命中时间及7天淘汰逻辑。旧缓存不再读取，不触碰 rules_local 或远程用户规则。
- 删除历史和受限两个显示开关及对应 ViewModel 状态。普通列表只展示本次扫描未发现访问限制的候选；正常类型范围匹配仍显示。
- 未匹配/受限的已配置组件只在“已选规则”中保留取消入口，不与普通列表混合。没有自动删除用户规则或按时间过期；取消此类规则后移除管理占位。普通组件在全部视图下勾选、取消均不改变可见性。
- 保留全部、分享、多文件分享、打开方式、浏览器、文本处理分类；保留搜索、图标、应用展开、按当前可见组件批量勾选和排序。
- 视图名称为“全部 / 已选规则 / 未选规则”，避免白名单或暂停模式下把勾选误称为“已隐藏”。分类、搜索仍一起生效。
- 规则页只显示简短运行提示，点击进入状态页；恢复/重置仅在异常确需选择时出现。不常驻更新检测、文件检查和技术说明。
- 更新检测、作用域详情和诊断工具在状态页。组件行不再展开大量匹配依据，详细数据保留在诊断包。
- 文件检查是独立诊断任务，不取消目录刷新，也不把结果混入管理目录；ZIP 保留最多5000条文件检查组件明细。

## 保留的后台保护

管理查询豁免、单JSON配置、system摘要确认、版本校验、损坏配置恢复、空结果保护、部分扫描失败提示和热重载错误处理继续工作。配置schema未变，安全暂停版本白名单增加21。未验证运行模块时仍不接受新扫描；会话内保留上次结果，重启应用后重新扫描，不以磁盘历史目录充当新结果。

## 回归验证

Kotlin回归更新为：普通组件勾选不消失；未匹配和受限规则只在已选视图可管理；分类/搜索仍约束已选视图；取消缺失规则不留下历史项；刷新用新扫描结果替换；宽泛匹配不误当受限；不重复添加已匹配规则。

独立Java检查覆盖分类、身份、过滤保护、缓冲边界和安全暂停协议。Android/Kotlin构建与真机UI尚需本地验证，不能将Java检查等同于APK或热重载验证。

## 升级

覆盖安装即可，无需清数据或重新设置规则。在状态页应用模块更新；不支持或失败则重启。查看 system 和已运行 Resolver 是否为 UP_TO_DATE/21，随后刷新。需要取消未匹配规则时切换“已选规则”。
