# 1.6.0 / 25 — Root 组件清理

按用户确认改为“实时扫描全部匹配组件、以系统状态为准”，不保存禁用名单，不提供自动全部启用，不增加新分类排序。

## 实现

- 新增 RootComponentCatalog，扫描启用与禁用的标准磁贴服务、快捷方式创建 Activity、小部件接收器。补充相应包可见性声明。
- 新增 ComponentStatePolicy：明确区分 DEFAULT/ENABLED/DISABLED，未知状态禁止操作。构造命令只允许严格校验后的完整组件名和当前用户，不接受任意 shell 参数，不启停整个包。
- 新增 ComponentRootCommand：用户确认后执行 su；25秒超时，16KiB 有界输出。拒绝授权/失败/超时不自动重试；再次读取 PackageManager 状态并刷新列表，不用缓存勾选假报成功。
- 新增“组件”页三分类。默认关闭或其他工具禁用的组件也显示，启用前提示无法恢复原状态。整体停用应用、核心 UID、android/SystemUI/管理应用组件仅展示。
- 删除旧 TileScreen、TileCatalog、TileEditorAccess 和对应旧编辑器测试，移除 SystemUI 磁贴 Hook 及其作用域推荐。旧磁贴序列化字段仅保留兼容备份，不自动执行或迁移成禁用。
- 保留原五类意图规则、排序、UID 绕过和配置验证。Root 组件页不受 Intent 的黑/白名单或 LSPosed 状态控制。
- 诊断增加最近扫描的组件状态及本会话最后一次操作记录，不保存恢复名单。

## 边界

禁用不是单纯隐藏：已固定磁贴、已放置小部件及共用组件的其他入口可能失效；恢复启用不保证恢复原布局。系统内置无独立组件的磁贴、动态快捷方式、ShortX 私有入口不保证支持。选择器缓存可能需要重开。

扫描与操作仅当前安装用户。状态由系统保存，清除 Intentcleaner 数据不会撤销禁用；重新扫描可继续管理。JSON 规则备份不携带 Root 组件状态。

## 验证

运行宿主 Java 回归；新增 ComponentStateCheck 覆盖默认关闭/显式启用/禁用/未知状态、组件命令注入防护、当前用户参数、进程成功/失败/超时，测试不执行 su 或 pm。

未配置 Android SDK/Kotlin 编译器，未编译 APK、未运行 Kotlin/Gradle 测试，未实机验证。升级后重启一次以移除旧 SystemUI Hook；本地自行编译。构建环境配置未改动，仅提升应用版本。
